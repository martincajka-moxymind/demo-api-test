# Example environment for local development
# Copy to .env and adjust values as needed. Never commit your real .env.

# Base URL of the target API (defaults to https://reqres.in/api if omitted)
API_BASE_URL=https://reqres.in/api

# Request timeout in ms (defaults to 5000 if omitted)
API_TIMEOUT=7000

# Optional API key. If provided, an x-api-key header will be sent.
API_KEY=replace-with-your-api-key

# Enable HTTP logging in tests ("1", "true", "on", or "debug")
LOG_HTTP=
